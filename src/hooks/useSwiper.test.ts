import { describe, it, expect } from 'vitest'
import { useSwiper } from './useSwiper'

describe('useSwiper', () => {
  it('exports a function', () => {
    expect(typeof useSwiper).toBe('function')
  })
})
